<htm>
  <body>
    <table align="center" class="table table-bordered">
      <h2 align="center" style="background-color:lightblue;">CUSTOMERS LIST</h2>
      <tr>
        <th>CustName</th>
        <th>CustPhone</th>
        <th>txtGmail</th>
        <th>Image</th>
      </tr>
      <%  for (var item of customers) { %>
      <tr>
        <td>
          <a href="/admin/customers=<%= item._id %>">
            <%= item._id %>
          </a>
        </td>
        <td><%= item.name %></td>
        <td><%= item.phone %></td>
        <td><%= item.gmail %></td>
        <td><img src="data:image/jpg;base64,<%= item.customers.image %>" width="70" height="70" /></td>
        <td><%= new Date(item.datetime).toLocaleString() %></td>
      </tr>
      <% } %>
    </table>
  </body>
</htm>

